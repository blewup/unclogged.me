/*
 * Custom styles for the privacy policy page.  See style_01.css for
 * explanation of the utilities defined here.  This file keeps the
 * navigation and card styling consistent across policy and terms pages.
 */

@font-face {
    font-family: 'Black Ops One';
    src: url('../fonts/BlackOps.ttf') format('truetype');
}
@font-face {
    font-family: 'Plus Jakarta Sans';
    src: url('../fonts/PlusJakartaSans.ttf') format('truetype');
}
.glass-nav {
    backdrop-filter: blur(12px);
    -webkit-backdrop-filter: blur(12px);
    transition: transform 0.3s ease-in-out;
}
.radius-override {
    border-radius: 15px !important;
}
.hover-scale:hover {
    transform: scale(1.05);
    transition: transform 0.2s;
}
html {
    scroll-behavior: smooth;
}

/*
 * Typography enhancements
 */
body {
    font-family: 'Inter', 'Plus Jakarta Sans', sans-serif;
}
h1, h2, h3, h4, h5, h6 {
    font-family: 'Playfair Display', 'Merriweather', serif;
}
pre, code {
    font-family: 'JetBrains Mono', monospace;
}
